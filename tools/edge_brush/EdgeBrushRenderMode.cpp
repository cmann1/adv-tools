enum EdgeBrushRenderMode
{
	
	/// Always render tiles edges while the edge brush is active
	Always,
	/// Only render tiles edges while drawing with the edge brush
	DrawOnly,
	/// Never render tiles edges
	Never,
	
}
