enum LayerInfoDisplay
{
	
	/// Don't show the layer info overlay.
	None,
	/// Show only the layer when changing layers, and only the sub layer when changing sub layers.
	Individual,
	/// Show LAYER.SUB_LAYER when changing either.
	Compound,
	
}
