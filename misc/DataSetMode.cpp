enum DataSetMode
{
	
	/// Simply updates/sets the value.
	Set,
	/// Stores the current value so it can be restored later.
	Store,
	/// Updates the value from the previously stored value.
	Restore,
	
}
